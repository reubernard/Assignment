#What is wrong with this code?

rating = input('Enter an integer rating between 1 and 10')

#This code collects the rating value as a string. Therefore it needs to convert it to an integer. hence an error
